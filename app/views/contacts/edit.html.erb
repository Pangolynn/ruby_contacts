<h1>Editing contact</h1>

<%= render "form", contact: @contact %>
<%= link_to "Delete", @contact, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: "btn mt-2 btn-outline-danger" %>
<br>

<div class="mt-3">
  <%= link_to "Show this contact", @contact, class:"link-dark link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover" %> |
  
  <%= link_to "Back to contacts", contacts_path, class:"link-dark link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover" %>
</div>
